import React from 'react';
import PropTypes from 'prop-types';

Admin.propTypes = {
    
};

function Admin(props) {
    return (
        <div>
            Hello
        </div>
    );
}

export default Admin;